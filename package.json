{
  "devDependencies": {
    "husky": "^2.3.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "workspaces": [
    "demo/*",
    "packages/*"
  ],
  "husky": {
    "hooks": {
      "pre-commit": "yarn workspaces run precommit"
    }
  },
  "prettier": {
    "semi": false
  }
}
